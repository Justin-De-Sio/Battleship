package model.ship;

import model.ship.Ship;

public class Submarine extends Ship {
    public Submarine(int x, int y, boolean isVertical, Powershot powershot, Length length) {
        super(x, y, isVertical, powershot, length);
    }
}
