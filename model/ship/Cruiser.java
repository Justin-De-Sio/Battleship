package model.ship;

import model.ship.Ship;

public class Cruiser extends Ship {
    public Cruiser(int x, int y, boolean isVertical, Powershot powershot, Length length) {
        super(x, y, isVertical, powershot, length);
    }
}
